

const initialState = {
  data:null
}
const doctorReducer = (state = initialState,action) =>{
  const {type,playload} = action
  switch (type){
    default:
      return state
  }
}

export default doctorReducer