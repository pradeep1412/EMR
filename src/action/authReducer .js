const initialState = {
  token:'fadlakjdfLAFH'
}
const authReducer = (state = initialState,action) =>{
  const {type,playload} = action
  switch (type){
    default:
      return state
  }
}

export default authReducer